import { SaleStatusIconPipe } from './sale-status-icon.pipe';

describe('SaleStatusIconPipe', () => {
  it('create an instance', () => {
    const pipe = new SaleStatusIconPipe();
    expect(pipe).toBeTruthy();
  });
});
